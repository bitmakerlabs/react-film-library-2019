import React from 'react';

const Fave = () => {
  return (
    <button className="film-row-fave add_to_queue">
      <i className="material-icons">add_to_queue</i>
    </button>
  );
};

export default Fave;